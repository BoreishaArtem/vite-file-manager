export default class Node {
    constructor(path, name, children, id, type) {
        this.path = path
        this.name = name
        this.children = children
        this.id = id
        this.type = type
    }
}